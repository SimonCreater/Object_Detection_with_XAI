# 객체검출 LLM Wiki — MCP Wiki Tool 의존성
# 설치:  py -m pip install -r requirements.txt
fastmcp>=3.4.0          # MCP 서버 프레임워크 (tools/list, tools/call)
mcp>=1.27.0             # Model Context Protocol 코어 (fastmcp 의존)
Flask>=3.1.0            # MVP 웹 GUI (뷰어 + 챗봇)
python-frontmatter>=1.1 # YAML front-matter 파싱/직렬화
PyYAML>=6.0             # front-matter 백엔드
Markdown>=3.8           # 본문 markdown → HTML 렌더 (GUI)
